#pragma once
//#include <queue>
//#include <cmath>
//#include "ships.hpp"

/*
namespace battleship_bebop {
  struct PathNode {
    GridPos pos;
    int facing_dir;
    int f_cost;
    bool operator>(const PathNode& other) const {
      return f_cost > other.f_cost;
    }
  };

  struct ParentTracker {
    GridPos pos;
    int dir;
  };
}
*/
/*
   class ShipPathFinding {
   private:
   std::priority_queue OpenList;
   bool [][] ClosedList;

   public:
   void initialize() {

   }


   }

   std::priority_queue OpenList;
   bool [][] ClosedList;
 */
